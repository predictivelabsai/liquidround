[pytest]
pythonpath = .
markers =
    e2e: Playwright browser-driven end-to-end tests (require a running server on :5007)
    integration: real-provider integration tests that may use paid APIs
# Default run excludes browser and paid-provider integration tests.
addopts = -m "not e2e and not integration"

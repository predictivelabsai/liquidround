[pytest]
markers =
    e2e: Playwright browser-driven end-to-end tests (require a running server on :5007)
# Default run excludes e2e — invoke explicitly with `pytest -m e2e` to include.
addopts = -m "not e2e"
